# Go kit stringsvc example

http://gokit.io/examples/stringsvc.html

# How to use

```sh
$ go run *.go
```

```sh
$ curl -XPOST -d'{"s":"hello, world"}' localhost:8080/uppercase
```

```sh
$ curl -XPOST -d'{"s":"hello, world"}' localhost:8080/count
```